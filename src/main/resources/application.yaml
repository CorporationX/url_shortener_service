spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password
  data:
    redis:
      host: localhost
      port: 6379
  task:
    scheduling:
      pool:
        size: 10

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8080

logging:
  level:
    root: info

hash:
  batch:
    size: 1000
  generator:
    thread:
      pool:
        size: 5
        queue:
          capacity: 100
  cache:
    min:
      size: 20

url:
  shortener:
    base-url: "https://int.com"

scheduler:
  cleaner:
    cron: "0 0 3 * * ?"  # Каждый день в 3:00