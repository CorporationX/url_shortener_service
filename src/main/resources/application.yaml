spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
      ttl: 5 # day

  mvc:
    servlet:
      path: /api/v1

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8080

logging:
  level:
    root: info

url:
  service:
    baseShortUrl: "/api/v1/shortener/urls/"
    #baseShortUrl: "http://api/v1/shortener/urls/"

thread:
  pool:
    corePoolSize: 5
    maxPoolSize: 5
    queueCapacity: 100

hash:
  generator:
    batchSize: 100
  local-cache:
    capacity: 50
    fillPercentage: 20

generate:
  cron: "0 */1 * * * *"
  pool:
    corePoolSize: 5
    maxPoolSize: 5
    queueCapacity: 100

clean:
  cron: "0 0 0 * * *"