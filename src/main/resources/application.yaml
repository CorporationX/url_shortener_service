spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/postgres?reWriteBatchedInserts=true
    username: user
    password: password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        jdbc:
          batch_size: ${BATCH_SIZE}
        order_inserts: true
        order_updates: true
        format_sql: true

  data:
    redis:
      port: 6379
      host: localhost

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

api-version: /api/v1

url-shortener-service:
  batch-size: ${BATCH_SIZE}
  max-hashes-in-store: 700
  hash-generator-thread-pool:
    corePoolSize: 2
    maxPoolSize: 5
    queueCapacity: 2
  executor-service:
    corePoolSize: 2
    maxPoolSize: 4
    keepAliveTime: 20
  minimum-hash-length-in-percent: 20
  clean-old-url-cron: "0 0 12 * * *"
  url-to-clean-older-than: 365d
  cache-lifetime: 30m

server:
  port: 8090

logging:
  level:
    root: info